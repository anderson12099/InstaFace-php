<form method="post" action="{{ path('comentario_imagen_delete', {'id': comentario_imagen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comentario_imagen.id) }}">
    <button class="btn">Delete</button>
</form>
